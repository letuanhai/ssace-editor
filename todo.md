- [x] replace the "View file as text" text viewer with a read-only Ace editor (default read-only like the original viewer) plus a toolbar button next to "Refresh the content" that toggles the read-only state
- [x] show ace command palette without any ace editor opened and focused (similar to `browse_ss`), move the extension actions from popup into the command palette, command name should be prefixed with `SS-Ext: `. if any ace editor (code/text) is focused then the editor specific commands should be shown in the command pallete and apply to that editor, otherwise if no editor is focused then do not show those commands (only show general commands like ss-ext commands)
- [ ] allow saving ace configuration: default dark and light theme, font size,..., and configure vim mode commands (if possible)